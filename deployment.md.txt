I’ll prepare a self-contained production Docker image for your VPS, add the required Next.js standalone output setting, and provide a deployment path using Docker + Nginx + HTTPS for `danang-dev.my.id`.
The Docker setup is now in place: a multi-stage, non-root production image plus a minimal `.dockerignore`. I’ll validate the Next standalone build and, if Docker is available locally, build the image itself.
The Next.js standalone build validates successfully. Docker is not installed in this local environment, so I cannot run the container build here; the runbook includes the exact build and smoke-test commands to execute on the VPS.
Created:
- [Dockerfile](file:///c:/Users/micha/OneDrive/Documents/Project/Personal/Practices/portfolio/Dockerfile) — multi-stage, non-root production image
- [.dockerignore](file:///c:/Users/micha/OneDrive/Documents/Project/Personal/Practices/portfolio/.dockerignore)
- [next.config.ts](file:///c:/Users/micha/OneDrive/Documents/Project/Personal/Practices/portfolio/next.config.ts) now uses `output: "standalone"`.

`npx tsc --noEmit` and `npx next build` pass. Docker is not installed locally, so build the container on your VPS.

## VPS deployment guide
Assumption: Ubuntu 22.04/24.04 VPS and domain `danang-dev.my.id`.

### 1. Configure DNS
Create an `A` record:

```text
danang-dev.my.id → YOUR_VPS_PUBLIC_IP
```

Wait until this resolves:

```bash
dig +short danang-dev.my.id
```

### 2. Prepare the VPS
Connect:

```bash
ssh your-user@YOUR_VPS_PUBLIC_IP
```

Update and install base packages:

```bash
sudo apt update
sudo apt upgrade -y
sudo apt install -y ca-certificates curl git nginx certbot python3-certbot-nginx
```

Open firewall ports:

```bash
sudo ufw allow OpenSSH
sudo ufw allow "Nginx Full"
sudo ufw enable
```

Also allow TCP ports `80` and `443` in your VPS provider firewall.

### 3. Install Docker
```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
```

```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

```bash
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker $USER
```

Log out and reconnect, then verify:

```bash
docker --version
docker run --rm hello-world
```

### 4. Upload or clone the project
Recommended: use Git.

```bash
sudo mkdir -p /opt/portfolio
sudo chown -R $USER:$USER /opt/portfolio
git clone YOUR_GIT_REPOSITORY_URL /opt/portfolio
cd /opt/portfolio
```

For a private repository, configure a deploy key on the VPS. Do not place GitHub tokens in the Dockerfile.

### 5. Build and run the production container
```bash
cd /opt/portfolio
docker build -t danang-portfolio:latest .
```

```bash
docker run -d \
  --name danang-portfolio \
  --restart unless-stopped \
  --publish 127.0.0.1:3000:3000 \
  danang-portfolio:latest
```

Verify the app locally on the VPS:

```bash
docker ps
docker logs --tail 100 danang-portfolio
curl -I http://127.0.0.1:3000
```

Keep port `3000` bound to `127.0.0.1`; Nginx will be the only public entry point.

### 6. Configure Nginx reverse proxy
Create the site config:

```bash
sudo nano /etc/nginx/sites-available/danang-dev.my.id
```

Paste:

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name danang-dev.my.id;

    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Frame-Options "DENY" always;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Enable it:

```bash
sudo ln -s /etc/nginx/sites-available/danang-dev.my.id /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 7. Enable HTTPS
```bash
sudo certbot --nginx \
  -d danang-dev.my.id \
  --redirect \
  --agree-tos \
  -m YOUR_EMAIL_ADDRESS \
  --no-eff-email
```

Verify renewal:

```bash
sudo certbot renew --dry-run
```

### 8. Production smoke test
```bash
curl -I https://danang-dev.my.id
curl -I https://danang-dev.my.id/robots.txt
curl -I https://danang-dev.my.id/sitemap.xml
curl -I https://danang-dev.my.id/opengraph-image
curl -I "https://danang-dev.my.id/assets/hero/cv/cv%232.docx.pdf"
```

Also manually test navigation, CV download, certification links, mobile rendering, and social-share preview.

### Updating later
```bash
cd /opt/portfolio
git pull --ff-only
docker build -t danang-portfolio:latest .
docker stop danang-portfolio
docker rm danang-portfolio
```

```bash
docker run -d \
  --name danang-portfolio \
  --restart unless-stopped \
  --publish 127.0.0.1:3000:3000 \
  danang-portfolio:latest
```

Finally:

```bash
docker logs --tail 100 danang-portfolio
```

Note: the existing ESLint 9 / legacy config warning still appears during `next build`, but TypeScript and the production build complete successfully.